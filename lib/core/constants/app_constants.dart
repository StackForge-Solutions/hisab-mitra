class AppConstants {
  const AppConstants._();

  static const String appName = 'HisaabMitra';
  static const Duration splashDelay = Duration(milliseconds: 1500);
  static const Duration preloadDelay = Duration(milliseconds: 1700);
  static const Duration parsingDelay = Duration(milliseconds: 2200);
  static const double screenPadding = 20;
  static const double cardRadius = 24;
}
